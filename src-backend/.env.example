# Vocistant 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# ============ AI Backend ============

# ASR 模型路径 (默认: src-backend/models/Fun-ASR-MLT-Nano-2512-4bit)
# ASR_MODEL_PATH=/path/to/your/model

# ASR 默认语言: zh (中文), en (英文), auto (自动检测)
ASR_DEFAULT_LANGUAGE=zh

# 后端服务端口
BACKEND_PORT=8765

# ============ LLM 文本校正 ============

# LLM 提供商: openai, ollama (都使用 OpenAI 兼容 API)
LLM_PROVIDER=ollama

# OpenAI API 密钥 (使用 openai 提供商时必填)
# LLM_API_KEY=sk-...

# API 基础地址 (ollama 默认: http://localhost:11434/v1)
# LLM_API_BASE=http://localhost:11434/v1

# LLM 模型名称
# OpenAI 默认: gpt-4o-mini
# LLM_MODEL=gpt-4o-mini

# LLM 请求超时 (秒)
LLM_TIMEOUT=10

# LLM 温度参数 (0-2, 越低越保守)
LLM_TEMPERATURE=0.3

# ============ Frontend ============

# WebSocket 服务地址
VITE_WS_URL=ws://127.0.0.1:8765/ws/audio

# ============ Tauri ============

# 开发模式
TAURI_DEBUG=true
